{{- if .Values.oegcontroller.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.oegcontroller.service.name }}
  namespace: {{ include "oeg.namespace" . }}
  labels:
    {{- include "oeg.controller.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.oegcontroller.service.type }}
  ports:
    - name: http
      port: {{ .Values.oegcontroller.service.port }}
      targetPort: {{ .Values.oegcontroller.service.targetPort }}
      protocol: TCP
      {{- if and (eq .Values.oegcontroller.service.type "NodePort") .Values.oegcontroller.service.nodePort }}
      nodePort: {{ .Values.oegcontroller.service.nodePort }}
      {{- end }}
  selector:
    {{- include "oeg.controller.selectorLabels" . | nindent 4 }}
{{- end }}
